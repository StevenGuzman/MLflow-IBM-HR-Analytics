## Ejecución de MLflow UI

Para iniciar la interfaz de MLflow localmente en el puerto 5001:

```bash
uv run mlflow ui --backend-store-uri ./mlruns --port 5001
Documentar el deployment del modelo. 
1. Requisitos previos 
Para el despliegue del proyecto es necesario que se tenga el docker 
2. Instrucciones para construir la imagen 
3. Intrucciones para ejecutar el contener 
comando exacto para ejecutarlo 
4. Verificación del despliegue 
